import { formatClipboard } from 'library/helpers/format-clipboard';
import { module, test } from 'qunit';

module('Unit | Helper | format clipboard');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatClipboard([42]);
  assert.ok(result);
});
